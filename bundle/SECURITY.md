# Security

WireGuard WebUI Lite 主要面向内网和自托管环境。建议部署后尽快修改默认密码，并限制 WebUI 访问来源。

## 建议

- 首次登录后在“系统管理 -> 账号安全”修改账号密码。
- 如需公网访问 WebUI，放在 HTTPS 反向代理后，并在配置文件中开启 `cookie_secure`。
- 定期备份 `/etc/wg-webui` 和 `/etc/wireguard`。
- 修改高级设置前先确认当前接口、地址池和站点路由。

## 说明

默认密码提醒只在登录页简洁提示，不阻止登录，也不会在每个页面重复显示。
